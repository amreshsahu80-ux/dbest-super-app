(function(){
'use strict';
const VERSION='3.0.0';
const cfg=window.DBEST_RUNTIME_CONFIG||{};
const BASE=String(cfg.supabaseUrl||'').replace(/\/$/,'');
const KEY=cfg.supabasePublishableKey||'';
const COM=BASE+'/functions/v1/vaahak-commerce-live';
const TK='dbest_vaahak_live_token';
let busy=false,last=null,filter='all';
function token(){try{return localStorage.getItem(TK)||''}catch(e){return''}}
function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function money(v){return '₹'+Math.round(Number(v||0)).toLocaleString('en-IN')}
function when(v){try{return v?new Date(v).toLocaleString('en-IN',{dateStyle:'medium',timeStyle:'short'}):'—'}catch(e){return String(v||'—')}}
async function api(){const t=token();if(!t)throw new Error('Please login again.');const r=await fetch(COM,{method:'POST',cache:'no-store',headers:{'apikey':KEY,'Authorization':'Bearer '+KEY,'Content-Type':'application/json','x-vaahak-token':t},body:JSON.stringify({action:'history',limit:100})});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'Unable to load completed transactions');return d}
function ensure(){const dash=document.getElementById('dash');if(!dash)return null;let card=document.getElementById('dbestCompletedTxFinal');if(card)return card;card=document.createElement('div');card.id='dbestCompletedTxFinal';card.className='card';card.style.border='2px solid #cfe0ff';card.innerHTML=`<div style="display:flex;justify-content:space-between;gap:10px;align-items:center;flex-wrap:wrap"><div><h3 style="margin:0">✅ Completed Rides & Deliveries</h3><div class="muted" style="margin-top:4px">Your completed transactions and earnings</div></div><button type="button" class="btn soft" id="dbestCompletedRefresh">Refresh</button></div><div id="dbestCompletedStats" class="stat" style="margin-top:12px"><div><small>Completed</small><b>—</b></div><div><small>Total Earnings</small><b>—</b></div></div><div class="row" id="dbestCompletedTabs" style="margin-top:12px"></div><div id="dbestCompletedList" style="margin-top:10px">Loading completed transactions…</div>`;
 const currentCard=dash.querySelector('.card:nth-of-type(2)');if(currentCard&&currentCard.nextSibling)dash.insertBefore(card,currentCard.nextSibling);else dash.appendChild(card);
 card.querySelector('#dbestCompletedRefresh').onclick=()=>load(true);return card}
function render(d){last=d;const card=ensure();if(!card)return;const jobs=Array.isArray(d.jobs)?d.jobs:[],s=d.summary||{};const stats=card.querySelector('#dbestCompletedStats');stats.innerHTML=`<div><small>Completed</small><b>${Number(s.completed??jobs.length)}</b></div><div><small>Cab Rides</small><b>${Number(s.rides??jobs.filter(j=>j.kind==='ride').length)}</b></div><div><small>Deliveries</small><b>${Number(s.deliveries??jobs.filter(j=>j.kind==='delivery').length)}</b></div><div><small>Total Earnings</small><b>${money(s.earnings??jobs.reduce((a,j)=>a+Number(j.partner_earning||0),0))}</b></div>`;
 const tabs=card.querySelector('#dbestCompletedTabs');tabs.innerHTML=`<button type="button" class="btn ${filter==='all'?'':'soft'}" data-f="all">All</button><button type="button" class="btn ${filter==='ride'?'':'soft'}" data-f="ride">Cab Rides</button><button type="button" class="btn ${filter==='delivery'?'':'soft'}" data-f="delivery">Deliveries</button>`;tabs.querySelectorAll('button').forEach(b=>b.onclick=()=>{filter=b.dataset.f||'all';render(last||d)});
 const rows=filter==='ride'?jobs.filter(j=>j.kind==='ride'):filter==='delivery'?jobs.filter(j=>j.kind==='delivery'):jobs;
 card.querySelector('#dbestCompletedList').innerHTML=rows.length?rows.map(j=>{const delivery=j.kind==='delivery',meta=j.order_meta||{},earning=Number(j.partner_earning||0),amount=delivery?Number(meta.collectAmount||j.fare||0):Number(j.fare||0),pay=String(j.payment_status||'')==='Prepaid'?'Prepaid':(['Received','Legacy Completed'].includes(String(j.payment_status||''))?'Received':String(j.payment_status||'—'));return `<div class="job" style="margin-top:10px;background:#fff"><div style="display:flex;justify-content:space-between;gap:8px"><b>${delivery?'📦 Marketplace Delivery':'🚕 Cab Ride'}</b><span style="font-size:11px;font-weight:900;color:#17633f">COMPLETED</span></div><div style="margin-top:6px">${esc(j.pickup)} → ${esc(j.dropoff)}</div><div style="display:grid;grid-template-columns:1fr 1fr;gap:7px;margin-top:9px"><div style="padding:9px;border-radius:10px;background:#f6f9ff"><small>${delivery?'Order / Collection':'Ride Fare'}</small><b style="display:block">${money(amount)}</b></div><div style="padding:9px;border-radius:10px;background:#eefaf3"><small>Your Earning</small><b style="display:block;color:#17633f">${money(earning)}</b></div><div style="padding:9px;border-radius:10px;background:#f7f9fc"><small>Payment</small><b style="display:block">${esc(pay)}</b></div><div style="padding:9px;border-radius:10px;background:#f7f9fc"><small>Mode</small><b style="display:block">${esc(j.payment_method||'—')}</b></div></div><div class="muted" style="margin-top:7px;font-size:12px">${esc(j.id)} • ${when(j.completed_at)}</div></div>`}).join(''):'<div class="muted" style="padding:12px 0">No completed transactions in this category yet.</div>'}
async function load(force=false){const dash=document.getElementById('dash');if(!dash||dash.classList.contains('hidden')||!token())return;ensure();if(busy)return;busy=true;try{render(await api())}catch(e){const el=document.getElementById('dbestCompletedList');if(el)el.innerHTML='<div class="bad" style="padding:10px;border-radius:10px">'+esc(e.message||'Unable to load completed transactions')+'</div>'}finally{busy=false}}
function watch(){const dash=document.getElementById('dash');if(dash&&!dash.classList.contains('hidden')&&token()){ensure();load(false)}}
new MutationObserver(watch).observe(document.documentElement,{subtree:true,attributes:true,attributeFilter:['class']});
setInterval(watch,4000);
setTimeout(watch,300);
window.DBEST_VAAHAK_COMPLETED_FINAL={version:VERSION,refresh:()=>load(true)};
})();