(function(){
'use strict';
const VERSION='3.4.0';
const cfg=window.DBEST_RUNTIME_CONFIG||{},BASE=String(cfg.supabaseUrl||'').replace(/\/$/,''),KEY=cfg.supabasePublishableKey||'',SYNC=BASE+'/functions/v1/marketplace-vaahak-sync',MKT=BASE+'/functions/v1/marketplace-live',HAND=BASE+'/functions/v1/marketplace-handover-live',COM=BASE+'/functions/v1/vaahak-commerce-live',TK='dbest_vaahak_live_token';
const assignedSynced=new Set(),arrived=new Set();
function token(){try{return localStorage.getItem(TK)||''}catch(e){return''}}
async function call(url,action,body={}){const h={apikey:KEY,'Content-Type':'application/json','x-vaahak-token':token()};if(String(KEY).startsWith('eyJ'))h.Authorization='Bearer '+KEY;const r=await fetch(url,{method:'POST',cache:'no-store',headers:h,body:JSON.stringify({action,...body})});const d=await r.json().catch(()=>({}));if(!r.ok){const e=new Error(d.error||'request_failed');e.data=d;throw e}return d}
function isMarketplaceDelivery(j){return !!(j&&j.kind==='delivery'&&j.order_meta&&j.order_meta.marketplaceOrderId)}
function isPaid(j){return ['Received','Prepaid','Paid','Razorpay Verified','Legacy Completed'].includes(String(j?.payment_status||''))}
function activeJob(){try{return typeof currentJob!=='undefined'?currentJob:null}catch(_){return null}}
async function syncStep(jobId,step){try{return await call(SYNC,'sync',{jobId,step})}catch(e){console.warn('Marketplace Vaahak sync',step,e);return null}}
async function handover(jobId){return await call(HAND,'vaahak_status',{jobId})}
async function markAssigned(j){if(!isMarketplaceDelivery(j)||assignedSynced.has(j.id)||!j.assigned_partner_id)return;assignedSynced.add(j.id);await syncStep(j.id,'assigned')}
async function markArrived(jobId,btn){if(btn){btn.disabled=true;btn.textContent='Updating…'}try{let d=await syncStep(jobId,'arrived');if(!d)d=await call(MKT,'vaahak_pickup_arrived',{jobId});arrived.add(jobId);if(btn){btn.textContent='✓ Reached Vendor';btn.classList.add('good')}try{typeof note==='function'&&note('Arrival confirmed. Collect the order after the Vendor taps “Handed to Vaahak”.')}catch(e){}}catch(e){if(btn){btn.disabled=false;btn.textContent='📍 Reached Vendor'}try{typeof note==='function'&&note('Could not confirm arrival: '+e.message,false)}catch(_){}}}
function marketplacePaymentHtml(j){const paid=isPaid(j),started=j.status==='Trip Started';if(!started&&!paid)return '<div class="paymentBox"><div class="paymentHead"><b>💳 Marketplace Payment</b><span class="paymentPill">RAZORPAY</span></div><div class="muted">Customer payment is collected through Razorpay after delivery starts. Cash / UPI / Other confirmation is not used for Marketplace orders.</div><div class="gps" style="margin-top:10px">After Vendor handover is confirmed, tap Start Delivery.</div></div>';return `<div class="paymentBox"><div class="paymentHead"><b>💳 Marketplace Payment</b><span class="paymentPill ${paid?'paid':''}">${paid?(j.payment_status==='Prepaid'?'PREPAID':'RAZORPAY VERIFIED'):'PENDING'}</span></div>${paid?`<div class="muted">${j.payment_status==='Prepaid'?'Payment was completed online before dispatch.':'Customer payment has been verified by DBest through Razorpay.'}</div><div class="gps" style="margin-top:10px">Complete delivery only after the Customer confirms receipt.</div>`:`<div class="muted">Waiting for the Customer to pay through Razorpay in the DBest order screen. No Cash / UPI / Other confirmation is required from the Vaahak.</div><div class="gps" style="margin-top:10px">This status refreshes automatically after successful payment.</div>`}</div>`}
const oldPaymentHtml=window.paymentHtml;
if(typeof oldPaymentHtml==='function')window.paymentHtml=function(j){if(isMarketplaceDelivery(j))return marketplacePaymentHtml(j);return oldPaymentHtml.apply(this,arguments)};
function ensureCompletionAction(j){const host=document.getElementById('jobs');if(!host)return;const existing=host.querySelector('[data-dbest-marketplace-complete]');const completed=/completed|delivered/i.test(String(j?.status||''));if(!isMarketplaceDelivery(j)||!isPaid(j)||completed){existing?.remove();return}if(existing)return;const wrap=document.createElement('div');wrap.dataset.dbestMarketplaceComplete=j.id;wrap.style.cssText='margin-top:12px;padding:12px;border:1px solid #b9d9ff;border-radius:15px;background:#eef7ff';wrap.innerHTML='<div style="font-weight:900;color:#173b78">✅ Payment verified</div><div style="margin-top:5px;font-size:12px;color:#4d6482">After the Customer taps “I Received My Order”, complete the delivery below.</div><button type="button" class="btn wide" data-dbest-complete-btn style="margin-top:10px">✅ Complete Delivery</button>';host.appendChild(wrap);const b=wrap.querySelector('[data-dbest-complete-btn]');if(b)b.onclick=()=>window.completeJob?.(j.id)}
function decoratePayment(j){if(!isMarketplaceDelivery(j))return;const host=document.getElementById('jobs'),box=host?.querySelector('.paymentBox');if(box){const wrap=document.createElement('div');wrap.innerHTML=marketplacePaymentHtml(j);const fresh=wrap.firstElementChild;if(fresh)box.replaceWith(fresh)}ensureCompletionAction(j)}
function decorate(j){if(!isMarketplaceDelivery(j))return;setTimeout(()=>decoratePayment(j),10);if(j.status!=='Accepted'||!j.assigned_partner_id)return;markAssigned(j);const host=document.getElementById('jobs');if(!host||host.querySelector('[data-dbest-vendor-arrived]'))return;const start=[...host.querySelectorAll('button')].find(b=>/Start Delivery|Picked Up/i.test(String(b.textContent||'')));if(!start)return;const b=document.createElement('button');b.type='button';b.className='btn soft';b.dataset.dbestVendorArrived=j.id;b.style.width='100%';b.style.marginTop='10px';b.textContent=arrived.has(j.id)?'✓ Reached Vendor':'📍 Reached Vendor';if(arrived.has(j.id))b.disabled=true;start.parentNode.insertBefore(b,start);b.onclick=()=>markArrived(j.id,b);const hint=document.createElement('div');hint.className='gps';hint.style.marginTop='7px';hint.textContent='No PIN needed. Vendor confirms the handover with one tap.';b.insertAdjacentElement('afterend',hint)}
const oldRender=window.renderJob;
if(typeof oldRender==='function')window.renderJob=function(j){const r=oldRender.apply(this,arguments);setTimeout(()=>decorate(j),20);return r};
const oldStart=window.startDelivery;
if(typeof oldStart==='function')window.startDelivery=async function(id){const j=activeJob();if(!isMarketplaceDelivery(j))return oldStart.apply(this,arguments);try{const s=await handover(id);if(!s.vendorConfirmed){try{typeof note==='function'&&note('Ask the Vendor to tap “Handed to Vaahak” first.',false)}catch(_){}return}const r=await oldStart.apply(this,arguments);setTimeout(()=>syncStep(id,'picked_up'),150);return r}catch(e){try{typeof note==='function'&&note('Pickup confirmation check failed. Please retry.',false)}catch(_){}}};
const oldPay=window.confirmPayment;
if(typeof oldPay==='function')window.confirmPayment=async function(id,method){const j=activeJob();if(isMarketplaceDelivery(j)){try{typeof note==='function'&&note('Marketplace payment is completed by the Customer through Razorpay. Please wait for verified payment status.',false)}catch(_){}decoratePayment(j);return}return oldPay.apply(this,arguments)};
const oldComplete=window.completeJob;
if(typeof oldComplete==='function')window.completeJob=async function(id){const j=activeJob();if(!isMarketplaceDelivery(j))return oldComplete.apply(this,arguments);try{if(!isPaid(j)){try{typeof note==='function'&&note('Waiting for Customer Razorpay payment verification.',false)}catch(_){}return}const s=await handover(id);if(!s.customerConfirmed){try{typeof note==='function'&&note('Ask the Customer to tap “I Received My Order” first.',false)}catch(_){}return}await call(COM,'complete_delivery',{jobId:id});try{typeof note==='function'&&note('Delivery completed successfully after verified Razorpay payment and Customer confirmation.')}catch(_){}setTimeout(()=>syncStep(id,'complete'),120);try{typeof loadStatus==='function'&&loadStatus(true)}catch(_){}}catch(e){try{typeof note==='function'&&note('Delivery completion check failed: '+e.message,false)}catch(_){}}};
new MutationObserver(()=>{const j=activeJob();if(isMarketplaceDelivery(j))decoratePayment(j)}).observe(document.documentElement,{childList:true,subtree:true});
setInterval(()=>{const j=activeJob();if(isMarketplaceDelivery(j))decoratePayment(j)},700);
window.DBEST_VAAHAK_MARKETPLACE_SYNC={version:VERSION,arrived:markArrived,sync:syncStep};
})();